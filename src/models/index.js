'use strict';

const ProjectsModel = require('./projects');
const TasksModel = require('./tasks');
const UsersModel = require('./users');

module.exports = {
    ProjectsModel,
    TasksModel,
    UsersModel
};